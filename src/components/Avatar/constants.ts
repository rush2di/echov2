import { IconProp } from "@fortawesome/fontawesome-svg-core";

const checkIcon: IconProp = ["fas", "check"];

const dropdownIcon: IconProp = ["fas", "chevron-down"];

const SM_SIZE = "sm";

const LG_SIZE = "lg";

export { SM_SIZE, LG_SIZE, checkIcon, dropdownIcon };
